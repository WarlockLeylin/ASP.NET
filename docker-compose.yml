version: '3.8'

services:
  task-service:
    build:
      context: ./Midterm
      dockerfile: Dockerfile
    container_name: midterm-task-service
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080
    restart: unless-stopped